# Orchestrate or schedule a set of jobs
jobs:
  Unit-Test:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run:
          name: Run Tests
          command: |
            docker-compose run unittests 
 
workflows:
  version: 2
  Test:
    jobs:
      - Unit-Test 
