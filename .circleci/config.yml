version: 2.1
jobs:
  test:
    machine: true
    steps:
      - checkout
      - run:
          name: Run Tests
          command: |
            docker-compose run unittests bash build-unittests.sh
  app-arm:
    machine: true
    steps:
      - run:
          name: Build arm application
          command: |
            docker-compose run app-arm bash build-app-x86.sh
            ls -al

workflows:
  test_and_build:
    jobs:
      - test
      - app-arm:
        requires:
          - test
